using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using Cinemachine;

public class CameraControl : Singleton<CameraControl>
{

    //private CinemachineVirtualCamera _camera;
    //private CinemachineTransposer _transposer;

    //public void SetTarget(Transform target, bool isGameOver = false)
    //{
    //    if (_camera == null)
    //    {
    //        _camera = GameObject.FindWithTag("VCamera").GetComponent<CinemachineVirtualCamera>();
    //        _transposer = _camera.GetCinemachineComponent<CinemachineTransposer>();
    //    }
    //    if (isGameOver)
    //    {
    //        _transposer.m_FollowOffset = new Vector3(0, 5 ,8);
    //    }
    //    _camera.Follow = target;
    //}
}
